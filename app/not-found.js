export default function NotFound() {
  return (
    <div className="min-h-screen flex items-center justify-center" style={{ background: 'linear-gradient(135deg, var(--silk-primary), var(--silk-secondary))' }}>
      <h1 style={{ color: 'var(--silk-dark)', fontSize: '2rem' }}>Page Not Found</h1>
    </div>
  );
}
